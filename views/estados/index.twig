{% extends "layout/default/template.twig" %}
{% block title %}
	{{options.title}}
{% endblock %}

{% block content %}
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                {{options.subject}}
                <a href="{{BASE}}{{options.link_create}}" class="btn btn-outline-dark btn-sm">{{options.button_create}}</a>
            </div>
            <div class="card-body">
                {% include "partials/_messages.twig" %}
                {% if options.estados is not empty %}
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th class="col-2">Id</th>
                                <th class="col-8">Estado</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for model in options.estados %}
                                <tr>
                                    <td>{{model.id}} </td>
                                    <td>{{model.nombre}} </td>
                                    <td>
                                        <a href="{{BASE}}estados/show/{{model.id}}" class="btn btn-success btn-sm">
                                        {% include "partials/_button_view.twig" %}
                                        </a>
                                        <a href="{{BASE}}estados/edit/{{model.id}}" class="btn btn-warning btn-sm">
                                            {% include "partials/_button_edit.twig" %}
                                        </a>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                {% else %}
                    <p class="text-info">{{options.warning}}
                    </p>
                {% endif %}

            </div>
        </div>
    </div>
{% endblock %}
